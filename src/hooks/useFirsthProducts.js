import React, { useEffect, useState } from 'react'

export const useFirsthProducts = ({data}) => {
    const [products, setProducts]= useState([])
    useEffect(()=>{
        
    },[data])
  return{

  }
}
